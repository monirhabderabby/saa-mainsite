import { User } from "@prisma/client";

interface Props {
  users: User[];
}

const EmployeeTableContainer = () => {
  return <div>EmployeeTableContainer</div>;
};

export default EmployeeTableContainer;
